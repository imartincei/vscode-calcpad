' Test file with macros (should trigger complex resolution)

' Define variables
x = 5
y = 10

' Define a macro
#def greeting$ = "Hello World"

' Define a macro with parameters
#def calculate$(a$; b$) = a$ + b$

' Valid usage:
result1 = x + y
message = greeting$
result2 = calculate$(x; y)

' Invalid usage (should be flagged):
error1 = undefinedVar
error2 = unknownMacro$